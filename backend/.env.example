# Environment Configuration Template for Sakshya AI

# Copy this file to backend/.env and fill in your actual credentials
# DO NOT commit backend/.env to version control (it contains secrets)

# ========================================
# Required: Google Gemini API Configuration
# ========================================

# Your actual Google Gemini API key
# Get it from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_actual_api_key_here_xyz123

# Model name (default: gemini-2.5-flash-lite)
# Other options: gemini-2.0-flash, gemini-pro
GEMINI_MODEL_NAME=gemini-2.5-flash-lite

# ========================================
# Optional: Remote OCR Configuration
# ========================================

# Remote PaddleOCR endpoint (for handwritten document/image OCR)
# If not set, OCR features will still work for PDFs via pdfplumber
# Example: https://mathewjosephparakka--paddleocr-vl-service-ocr-api.modal.run/
# PADDLE_OCR_URL=https://your-paddle-ocr-endpoint.com

# ========================================
# Optional: Local Tesseract OCR Path
# ========================================

# Only needed if Tesseract OCR is installed in a non-standard location
# Windows example: C:\Program Files\Tesseract-OCR\tesseract.exe
# macOS example: /usr/local/Cellar/tesseract/5.x.x/bin/tesseract
# Linux example: /usr/bin/tesseract
# TESSERACT_CMD=/path/to/tesseract.exe

# ========================================
# Optional: Logging Configuration
# ========================================

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# ========================================
# Optional: Development Settings
# ========================================

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173

# ========================================
# Notes
# ========================================

# 1. Save this file as: backend/.env
# 2. Fill in your GEMINI_API_KEY with your actual API key
# 3. The .env file is ignored by git (.gitignore) - never commit secrets!
# 4. Only required field is GEMINI_API_KEY
# 5. Everything else is optional with sensible defaults
#
# Getting a Gemini API Key:
#   - Go to https://aistudio.google.com/app/apikey
#   - Click "Get API Key" or "Create API Key"
#   - Copy the API key
#   - Paste it as the value for GEMINI_API_KEY above
#
# Free Tier Limits:
#   - 15 requests per minute
#   - 500,000 tokens per day
#   - For production, upgrade to a paid plan
#   - Monitor usage at: https://aistudio.google.com/app/usage
